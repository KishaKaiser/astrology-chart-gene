# Environment Variables
# Copy this file to .env and fill in the values.
# This app uses GitHub Spark's cloud KV store for data persistence,
# so no database connection string is required for standard development.

# Optional: Override the Vite dev server port (default: 5173)
# VITE_PORT=5173

# Optional: Project root override used by vite.config.ts
# PROJECT_ROOT=
